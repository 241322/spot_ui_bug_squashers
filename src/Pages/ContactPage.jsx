import "../Styles/ContactStyle.css";

export default function ContactPage() {
    return(
        <div>
            <h1>Contact Us</h1>
            <p>Don't really, we don't care about your problems :)</p>
        </div>
    );
}